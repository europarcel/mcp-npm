# Europarcel API Configuration
EUROPARCEL_API_KEY=your-api-key-here

# Transport Configuration
# MCP_TRANSPORT=stdio          # stdio (default) for Claude Desktop, http for web services
# MCP_PORT=3000                # Port for HTTP mode (default: 3000)

# Rate Limiting (HTTP mode only)
# RATE_LIMIT_PER_MINUTE=50     # Requests per minute (default: 50)
# RATE_LIMIT_PER_HOUR=500      # Requests per hour (default: 500)

# Redirect URL for GET requests (HTTP mode only)
# REDIRECT_URL=https://your-company.com

# Logging
# LOG_LEVEL=info               # debug, info, warn, error (default: info)